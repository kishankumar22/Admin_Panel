BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Faculty] ADD [documents] NVARCHAR(1000),
[monthlySalary] INT,
[yearlyLeave] INT;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
